# CoSer Browser for Termux

CoSer Browser is a lightweight Bash script designed for [Termux](https://termux.dev/) that lets you quickly open websites in your default Android browser using the `termux-open-url` command. It also keeps a simple browsing history for reference.

---

## ✨ Features
- Open any website directly from Termux with a single command.
- Automatically adds `https://` if missing.
- Colorful output for better readability.
- Logs browsing history to `~/.coser_history`.
- Minimal and fast — perfect for quick access.

---

## 📦 Installation
1. Clone this repository:
   ```bash
   git clone https://github.com/joelclaude0817-beep/coser-browser.git
   cd coser-browser
